# Roadmap

## Task ladder (must be followed in order)
A) Bootstrap workspace + CI + formatting
B) Parser + AST + diagnostics
C) Units system + dimensional checks
D) Property facade trait + mock provider (tests)
E) CoolProp provider + caching
F) Nonlinear solver + scaling + convergence report
G) Parametric tables + persistence
H) Tauri UI (editor, vars, diagnostics, table, plot, open/save)

## Definition of Done for each task
- PR exists with clear description
- Tests added/updated
- `cargo test` green
- Minimal scope: task only
